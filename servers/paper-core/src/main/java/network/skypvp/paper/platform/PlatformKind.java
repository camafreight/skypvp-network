package network.skypvp.paper.platform;

/**
 * Detected server runtime. Plugins should not branch on this directly — use {@link ServerPlatform} instead.
 */
public enum PlatformKind {
   PAPER,
   FOLIA
}
